{
  "name": "@ediocracy/guide",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "pnpm run check && astro build",
    "preview": "astro preview",
    "check": "pnpm run test && pnpm run check:js && astro check && pnpm run check:examples && pnpm run check:content",
    "check:js": "tsc --ignoreConfig --noEmit --allowJs --checkJs --strict --module nodenext --target es2022 --types node src/lib/examples.mjs src/lib/walkthrough.mjs",
    "check:examples": "node scripts/check-examples.mjs",
    "check:content": "node scripts/check-content.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "0.42.2",
    "astro": "7.3.3",
    "sharp": "0.35.4"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
