# Legacy generic-X12 fixture material

`fixtures-v0.json` is the 22-record manifest that existed before Phase 4 standardization.
It is retained for provenance and is not the canonical manifest.

The canonical manifest retains legacy payload IDs 004, 006, 008, 011, 012, 013, 014, 015, and 021.
The remaining legacy payloads overlap a Phase 4 canonical case and remain on disk pending separately authorized cleanup.
